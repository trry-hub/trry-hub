# 💻 系统安装后要做的那些事

## 一、常用软件安装（所有平台通用）

### 🌲 Node.js 环境管理（NVM）
:::tip 提示
建议先安装 `jq` 工具，用于更好的 JSON 解析体验：
```bash
# macOS
brew install jq
# Ubuntu/Debian
sudo apt install jq
# CentOS/RHEL
sudo yum install jq
```
:::

1. 安装 [NVM (Node Version Manager)](https://github.com/nvm-sh/nvm)
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

2. 配置 NVM 环境变量
```bash
# ~/.zshrc 或 ~/.bashrc
# NVM 配置
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
```

3. 安装和使用 Node.js
```bash
# 安装最新的 LTS 版本
nvm install --lts
# 使用指定版本
nvm use 18
# 设置默认版本
nvm alias default 18
# 查看已安装版本
nvm ls
```

4. 安装 pnpm（推荐）
```bash
npm install -g pnpm
```

5. （推荐）Node.js 自动版本切换功能 ⭐

### 最优方案：智能版本切换（推荐）

将以下代码添加到 `~/.zshrc`：

```bash
# =============================================================================
# 自动切换 Node.js 版本 (基于 package.json engines.node)
# =============================================================================

# 自动切换 Node.js 版本函数
auto_switch_node() {
  # 检查是否存在 package.json
  if [[ ! -f package.json ]]; then
    return 0
  fi

  # 检查 nvm 是否可用
  if ! command -v nvm >/dev/null 2>&1; then
    echo -e "\033[33m⚠️  nvm 未安装，无法自动切换 Node.js 版本\033[0m" >&2
    return 1
  fi

  # 获取当前 Node.js 版本
  local current_version=$(node -v 2>/dev/null | sed 's/^v//' || echo "")
  
  # 解析 package.json 中的 engines.node
  local node_range=$(node -e "
    try {
      const fs = require('fs');
      const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
      console.log(pkg.engines?.node || '');
    } catch (e) {
      console.log('');
    }
  " 2>/dev/null)

  # 如果没有指定版本范围，直接返回
  if [[ -z "$node_range" ]]; then
    return 0
  fi

  # 简化版本范围解析 - 处理常见的格式
  local target_version=""
  
  # 对于 >=X.Y.Z 格式
  if [[ "$node_range" =~ ^\>\=[[:space:]]*([0-9]+\.[0-9]+\.[0-9]+) ]]; then
    target_version="${match[1]}"
  # 对于 ^X.Y.Z 格式
  elif [[ "$node_range" =~ ^\^([0-9]+\.[0-9]+\.[0-9]+) ]]; then
    target_version="${match[1]}"
  # 对于 ~X.Y.Z 格式
  elif [[ "$node_range" =~ ^\~([0-9]+\.[0-9]+\.[0-9]+) ]]; then
    target_version="${match[1]}"
  # 对于 X.Y.Z 格式
  elif [[ "$node_range" =~ ^([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
    target_version="${match[1]}"
  # 对于 X.Y.x 格式
  elif [[ "$node_range" =~ ^([0-9]+\.[0-9]+)\.x$ ]]; then
    target_version="${match[1]}.0"
  fi

  # 检查当前版本是否满足要求
  if [[ -n "$current_version" && -n "$target_version" ]]; then
    # 拆分当前版本号进行比较
    local current_major=$(echo "$current_version" | cut -d. -f1)
    local current_minor=$(echo "$current_version" | cut -d. -f2)
    local current_patch=$(echo "$current_version" | cut -d. -f3)
    local target_major=$(echo "$target_version" | cut -d. -f1)
    local target_minor=$(echo "$target_version" | cut -d. -f2)
    local target_patch=$(echo "$target_version" | cut -d. -f3)
    
    local current_satisfies=false
    
    # 对于 >=X.Y.Z 格式，检查当前版本是否满足要求
    if [[ "$node_range" =~ ^\>\= ]]; then
      if [[ "$current_major" -gt "$target_major" ]]; then
        current_satisfies=true
      elif [[ "$current_major" -eq "$target_major" ]]; then
        if [[ "$current_minor" -gt "$target_minor" ]]; then
          current_satisfies=true
        elif [[ "$current_minor" -eq "$target_minor" ]]; then
          if [[ "$current_patch" -ge "$target_patch" ]]; then
            current_satisfies=true
          fi
        fi
      fi
    else
      # 对于其他格式，检查当前版本是否匹配
      if [[ "$current_version" == "$target_version"* ]]; then
        current_satisfies=true
      fi
    fi
    
    # 如果当前版本满足要求，直接返回
    if [[ "$current_satisfies" == "true" ]]; then
      return 0
    fi
  fi

  # 获取已安装的 Node.js 版本列表
  local installed_versions=$(nvm ls --no-alias | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | sed 's/^v//' | sort -V)

  # 查找满足条件的版本
  local found_version=""
  if [[ -n "$target_version" ]]; then
    # 对于 >=X.Y.Z，查找大于等于该版本的最新版本
    if [[ "$node_range" =~ ^\>\= ]]; then
      while IFS= read -r version; do
        # 拆分版本号进行比较
        local current_major=$(echo "$version" | cut -d. -f1)
        local current_minor=$(echo "$version" | cut -d. -f2)
        local current_patch=$(echo "$version" | cut -d. -f3)
        local target_major=$(echo "$target_version" | cut -d. -f1)
        local target_minor=$(echo "$target_version" | cut -d. -f2)
        local target_patch=$(echo "$target_version" | cut -d. -f3)
        
        # 比较主版本号
        if [[ "$current_major" -gt "$target_major" ]]; then
          found_version="$version"
        elif [[ "$current_major" -eq "$target_major" ]]; then
          # 比较次版本号
          if [[ "$current_minor" -gt "$target_minor" ]]; then
            found_version="$version"
          elif [[ "$current_minor" -eq "$target_minor" ]]; then
            # 比较修订版本号
            if [[ "$current_patch" -ge "$target_patch" ]]; then
              found_version="$version"
            fi
          fi
        fi
      done <<< "$installed_versions"
    else
      # 对于其他格式，直接查找匹配的版本
      while IFS= read -r version; do
        if [[ "$version" == "$target_version"* ]]; then
          found_version="$version"
        fi
      done <<< "$installed_versions"
    fi
  fi

  # 如果找到满足条件的版本，切换到该版本
  if [[ -n "$found_version" ]]; then
    echo -e "\033[34m🔄 切换到 Node.js v$found_version (满足 $node_range)\033[0m"
    nvm use "$found_version" >/dev/null 2>&1
    return $?
  fi

  # 如果没有找到满足条件的版本，尝试自动安装
  echo -e "\033[33m⚠️  未找到满足 $node_range 的已安装 Node.js 版本\033[0m" >&2

  # 尝试安装目标版本
  if [[ -n "$target_version" ]]; then
    echo -e "\033[36m📦 正在安装 Node.js v$target_version...\033[0m"
    if nvm install "$target_version" >/dev/null 2>&1; then
      echo -e "\033[32m✅ Node.js v$target_version 安装成功\033[0m"
      nvm use "$target_version" >/dev/null 2>&1
      return $?
    else
      echo -e "\033[31m❌ Node.js v$target_version 安装失败\033[0m" >&2
    fi
  fi

  echo -e "\033[31m❌ 无法自动切换到满足 $node_range 的 Node.js 版本\033[0m" >&2
  echo -e "\033[33m💡 请手动安装: nvm install --lts 或 nvm install <version>\033[0m" >&2
  return 1
}

# 添加到 chpwd 钩子，在切换目录时自动执行
autoload -U add-zsh-hook
add-zsh-hook chpwd auto_switch_node

# 在终端启动时也执行一次
auto_switch_node
```

### 使用说明

1. **在项目 `package.json` 中定义版本**：
```json
{
  "engines": {
    "node": ">=20.11.0"
  }
}
```

2. **触发时机**：
   - **每次打开终端时**：自动检查当前目录的 package.json
   - **每次切换目录时**：自动检查新目录的 package.json
   - **手动执行**：在任何目录运行 `auto_switch_node` 命令

3. **版本切换逻辑**：
   - 如果当前版本满足要求 → 不执行任何操作
   - 如果当前版本不满足要求 → 自动切换到满足条件的最新版本
   - 如果没有满足条件的版本 → 尝试自动安装

### 特性

- ✅ **智能版本解析**：支持 `>=20.11.0`、`^18.0.0`、`~16.14.0`、精确版本和 `X.Y.x` 格式
- ✅ **最佳版本选择**：从已安装版本中选择最新的满足条件的版本
- ✅ **自动安装功能**：如果没有满足条件的版本，会尝试自动安装
- ✅ **清晰反馈**：提供彩色输出，显示切换状态和相关信息
- ✅ **错误处理**：完善的错误处理和用户提示
- ✅ **性能优化**：只在必要时进行版本切换，避免不必要的操作

### 工作原理

1. **检测阶段**：检查当前目录是否有 `package.json` 文件
2. **解析阶段**：读取并解析 `engines.node` 字段中的版本范围
3. **验证阶段**：检查当前 Node.js 版本是否满足要求
4. **查找阶段**：如果当前版本不满足，获取已安装版本列表并筛选满足条件的版本
5. **选择阶段**：从满足条件的版本中选择最新的一个
6. **切换阶段**：自动切换到选中的版本
7. **安装阶段**：如果没有满足条件的版本，尝试自动安装目标版本

### 示例场景

```bash
# 项目 A 要求 >=20.11.0，当前版本 v18.20.1
🔄 切换到 Node.js v24.11.1 (满足 >=20.11.0)

# 项目 B 要求 >=18.0.0，当前版本 v18.20.1
# (无输出，因为当前版本已满足要求)

# 项目 C 要求 ^14.17.2，当前版本 v18.20.1
🔄 切换到 Node.js v14.17.2 (满足 ^14.17.2)
```

### 重要说明

1. **zsh 正则表达式语法**：在 zsh 中，正则表达式需要使用引号包围，如 `"^\^([0-9]+\.[0-9]+\.[0-9]+)"`
2. **子 shell 限制**：在测试脚本或子 shell 中，`nvm use` 不会影响父 shell 的环境，这是正常行为
3. **实际使用**：在交互式终端中，版本切换会正常生效
4. **版本匹配逻辑**：
   - `>=X.Y.Z`：大于等于指定版本
   - `^X.Y.Z`：兼容指定版本的主版本，主版本号相同，次版本号可升级
   - `~X.Y.Z`：兼容指定版本的主次版本，主次版本号相同，补丁版本可升级
   - `X.Y.Z`：精确匹配
   - `X.Y.x`：匹配任意补丁版本

---

### 备选方案：NVM 原生支持

如果不需要解析 engines.node，可以使用 NVM 原生功能：

```bash
# ~/.zshrc - NVM 原生版本
autoload -U add-zsh-hook
add-zsh-hook chpwd '[[ -f .nvmrc || -f package.json ]] && nvm use 2>/dev/null'
```

需要在项目根目录创建 `.nvmrc` 文件：
```
20
```

### 方案对比

| 特性 | 智能版本切换 | NVM 原生 |
|------|-------------|----------|
| 代码量 | 完整实现 | 3 行 |
| 依赖工具 | NVM + Node.js | 仅 NVM |
| 配置文件 | package.json | .nvmrc |
| 版本解析 | 智能解析语义版本 | 需手动指定 |
| 版本选择 | 自动选择最新版本 | 固定版本 |
| 自动安装 | 支持自动安装 | 不支持 |
| 用户反馈 | 彩色输出提示 | 静默执行 |

**推荐**：使用智能版本切换，功能更强大，使用更便捷

6. 打印 Node、npm、pnpm 版本
```bash
# ~/.zshrc
echo "\033[1m\033[32mNode\033[0m $(node -v 2>/dev/null || echo '未安装') · \033[34mnpm\033[0m $(npm -v 2>/dev/null || echo '未安装') · \033[35mpnpm\033[0m $(pnpm -v 2>/dev/null || echo '未安装')"
```

### 🔒 VPN 工具
- **macOS**：[anyconnect-macos](https://cdnfile.yaomaitong.cn/vpn/anyconnect-macos-4.9.04053.dmg)（仅勾选 VPN）  
- **Windows**：[anyconnect_win.zip](https://cdnfile.yaomaitong.cn/vpn/anyconnect_win.zip)  
- **iPhone**：App Store 搜索 **AnyConnect**  
- **Android**：[OpenConnect.apk](https://cdnfile.yaomaitong.cn/vpn/OpenConnect-1.15-1152.apk)  

:::warning 提示
使用 VPN 前先安装个人 `.p12` 文件，然后连接：`vpn.yaomaitong.cn:4433`
:::

### 🖱️ 辅助工具
- Windows 鼠标点击效果：[ClickShow](https://github.com/cuiliang/ClickShow/releases/tag/1.4.1)  
- macOS 鼠标指针：[macOS-cursor](https://zhutix.com/ico/macos-cursor-21/)  
- Windows 鼠标指针：[Windows 指针主题](https://zhutix.com/ico/macos-cursor-21/)  

## 二、分平台配置方案

### 🔹 Windows

#### 1. 开机自启动
路径：  
```text
C:\ProgramData\Microsoft\Windows\Start Menu\Programs\StartUp
```
👉 建议将 **uTools**、**Snipaste** 等开机自启动的软件快捷方式放入其中。  

#### 2. PowerShell 脚本执行权限
以管理员身份运行 PowerShell：  
```powershell
set-executionpolicy remotesigned
```

#### 3. 安装 WSL
1. 参考安装教程：https://bbs.huaweicloud.com/blogs/350759  
2. 重启后，在 Microsoft Store 中安装：  
  [Windows Subsystem for Linux](https://apps.microsoft.com/store/detail/windows-subsystem-for-linux/9P9TQF7MRM4R)  

#### 4. PowerShell 美化（Oh My Posh）
1. 安装 [Oh My Posh](https://ohmyposh.dev/)  
2. 写入 `$PROFILE`：  
```powershell
oh-my-posh init pwsh --config $env:POSH_THEMES_PATH\M365princess.omp.json | Invoke-Expression
```
3. 字体推荐：**CaskaydiaCove NFM**  
4. 查看主题：  
```powershell
Get-PoshThemes
```

---

### 🔹 macOS

#### 1. Homebrew
安装 [Homebrew](https://brew.sh/)，便于软件包管理。  

#### 2. Zsh 美化
推荐使用 **[Starship](https://starship.rs/)**：  

```bash
brew install starship
```
zsh 配置：
```bash
# ~/.zshrc
# Starship 初始化
if command -v starship >/dev/null 2>&1; then
  eval "$(starship init zsh)"
fi
```
执行 `source ~/.zshrc` 使配置生效

##### 配置 Starship
创建 `~/.config/starship.toml` 文件并添加以下内容：
```toml
# ~/.config/starship.toml
format = """
$username\
$git_branch\
$nodejs\
$character"""
# 在提示符之间插入空行
add_newline = false

# 使用内置的username模块而不是env_var
[username]
style_user = "#17A8CD"
format = "[$user](#17A8CD):"
show_always = true

[git_branch]
# 显示当前的 Git 分支
format = "[$branch](#D670D6) "

[nodejs]
# 显示 Node.js 版本
format = "[$version](#23D184) "

[character]
# 确定提示符字符的显示，根据上次命令的成功或失败显示不同的颜色
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"
```

此配置会显示用户名、Git分支、Node.js版本，并根据命令执行结果显示不同颜色的提示符。

---

### 🔹 Linux (Ubuntu / Debian 示例)

#### 1. 更新系统
```bash
sudo apt update && sudo apt upgrade -y
```

#### 2. 安装常用工具
```bash
sudo apt install -y curl git vim build-essential
```

#### 3. 安装 Zsh + Starship
```bash
sudo apt install -y zsh
sh -c "$(curl -fsSL https://starship.rs/install.sh)"
echo 'eval "$(starship init zsh)"' >> ~/.zshrc
chsh -s $(which zsh)
```

#### 4. Node.js 环境配置
按照第一部分中的 NVM 安装指南配置 Node.js 环境。  

---

## 三、字体推荐
- [MiSans](https://web.vip.miui.com/page/info/mio/mio/detail?postId=33935854)  
- [Recursive](https://github.com/arrowtype/recursive)  
- CaskaydiaCove NFM （推荐给 PowerShell / VSCode 使用）

---

## ✅ 总结
本指南分为 **Windows / macOS / Linux 三套独立方案**：

- 🪟 Windows → PowerShell + WSL + Oh My Posh
- 🍎 macOS → Zsh + Starship + NVM Node 管理
- 🐧 Linux → Zsh + Starship + 系统工具

按步骤逐一执行，即可快速完成新机器的开发环境初始化。
