# 💻 系统安装后要做的那些事

## 一、常用软件安装（所有平台通用）

### 🌲 Node.js 环境管理（NVM）
:::tip 提示
建议先安装 `jq` 工具，用于更好的 JSON 解析体验：
```bash
# macOS
brew install jq
# Ubuntu/Debian
sudo apt install jq
# CentOS/RHEL
sudo yum install jq
```
:::

1. 安装 [NVM (Node Version Manager)](https://github.com/nvm-sh/nvm)
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

2. 配置 NVM 环境变量
```bash
# ~/.zshrc 或 ~/.bashrc
# NVM 配置
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
```

3. 安装和使用 Node.js
```bash
# 安装最新的 LTS 版本
nvm install --lts
# 使用指定版本
nvm use 18
# 设置默认版本
nvm alias default 18
# 查看已安装版本
nvm ls
```

4. 安装 pnpm（推荐）
```bash
npm install -g pnpm
```

5. （推荐）NVM 自动版本切换功能
使用 semver 命令行工具实现根据项目 `package.json` 的 `engines.node` 字段自动切换 Node.js 版本：

**🌟 强大功能**：
- 使用现成的 `semver` 命令行工具，与 npm 的 engines 字段验证完全一致
- 支持所有标准版本表达式：`>=18.0.0`、`^18.0.0`、`18.0.0 || 20.0.0` 等
- 智能版本选择：优先使用已安装的兼容版本
- 详细状态反馈：清晰的切换提示和错误处理

### 安装 semver 命令行工具

```bash
npm install -g semver
```

### 配置自动切换

将以下代码添加到 `~/.zshrc`：

```bash
# Auto NVM Version Switcher - 使用 semver 命令行工具
# 根据当前项目的 package.json engines 字段自动切换 Node.js 版本
autoload -U add-zsh-hook
add-zsh-hook chpwd auto_nvm_switch_semver

auto_nvm_switch_semver() {
    # 查找 package.json 文件
    if [[ -f package.json ]]; then
        local node_version=""
        if command -v jq >/dev/null 2>&1; then
            node_version=$(jq -r '.engines.node // empty' package.json 2>/dev/null)
        else
            node_version=$(grep -A2 '"engines"' package.json | grep '"node"' | sed 's/.*"node": *"\([^"]*\)".*/\1/')
        fi
        
        if [[ -n "$node_version" && "$node_version" != "null" ]]; then
            local current_version=$(node --version 2>/dev/null | sed 's/^v//')
            
            # 使用 semver 命令行工具检查当前版本是否满足要求
            if command -v semver >/dev/null 2>&1; then
                # 检查当前版本是否满足要求
                if semver -r "$node_version" "$current_version" >/dev/null 2>&1; then
                    return 0  # 当前版本满足要求，无需切换
                fi
                
                # 获取所有已安装的版本
                local installed_versions=$(nvm ls --no-colors | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | sed 's/^v//' | sort -V)
                
                # 找到满足条件的最高版本
                local best_version=""
                for version in $installed_versions; do
                    if semver -r "$node_version" "$version" >/dev/null 2>&1; then
                        best_version="$version"
                    fi
                done
                
                if [[ -n "$best_version" ]]; then
                    # 使用已安装的版本
                    if [[ "$current_version" != "$best_version" ]]; then
                        echo "🔄 Switching to Node.js v$best_version (engines: $node_version)"
                        nvm use "$best_version" >/dev/null 2>&1
                        echo "✅ Switched to Node.js $(node --version)"
                    fi
                else
                    # 没有已安装的版本满足条件，计算需要安装的版本
                    local target_version=""
                    
                    # 使用 semver 命令行工具解析版本表达式
                    case "$node_version" in
                        ">="*)
                            target_version=${node_version#">="}
                            ;;
                        ">"*)
                            target_version=${node_version#">"}
                            ;;
                        "^"*)
                            target_version=${node_version#"^"}
                            ;;
                        "~"*)
                            target_version=${node_version#"~"}
                            ;;
                        "||"*)
                            # 对于 OR 表达式，选择最高版本
                            target_version=$(echo "$node_version" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | sort -V | tail -1)
                            ;;
                        *)
                            # 尝试提取具体版本号
                            target_version=$(echo "$node_version" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
                            ;;
                    esac
                    
                    if [[ -n "$target_version" ]]; then
                        echo "📦 Installing Node.js v$target_version (engines: $node_version)"
                        nvm install "$target_version" >/dev/null 2>&1
                        if [[ $? -eq 0 ]]; then
                            nvm use "$target_version" >/dev/null 2>&1
                            echo "✅ Installed and switched to Node.js $(node --version)"
                        else
                            echo "❌ Failed to install Node.js v$target_version"
                        fi
                    fi
                fi
            fi
        fi
    fi
}

# 初始调用
auto_nvm_switch_semver
```

### 优势

1. **使用现成的工具**：semver 命令行工具是专门用于版本解析的工具，经过充分测试
2. **易于维护**：不需要自己实现复杂的版本解析逻辑
3. **与 npm 一致**：使用与 npm 相同的版本解析规则
4. **功能强大**：支持所有标准的版本表达式
5. **代码简洁**：相比自定义实现，代码量减少约 80%

6. 打印 Node、npm、pnpm 版本
```bash
# ~/.zshrc
echo "\033[1m\033[32mNode\033[0m $(node -v 2>/dev/null || echo '未安装') · \033[34mnpm\033[0m $(npm -v 2>/dev/null || echo '未安装') · \033[35mpnpm\033[0m $(pnpm -v 2>/dev/null || echo '未安装')"
```

### 🔒 VPN 工具
- **macOS**：[anyconnect-macos](https://cdnfile.yaomaitong.cn/vpn/anyconnect-macos-4.9.04053.dmg)（仅勾选 VPN）  
- **Windows**：[anyconnect_win.zip](https://cdnfile.yaomaitong.cn/vpn/anyconnect_win.zip)  
- **iPhone**：App Store 搜索 **AnyConnect**  
- **Android**：[OpenConnect.apk](https://cdnfile.yaomaitong.cn/vpn/OpenConnect-1.15-1152.apk)  

:::warning 提示
使用 VPN 前先安装个人 `.p12` 文件，然后连接：`vpn.yaomaitong.cn:4433`
:::

### 🖱️ 辅助工具
- Windows 鼠标点击效果：[ClickShow](https://github.com/cuiliang/ClickShow/releases/tag/1.4.1)  
- macOS 鼠标指针：[macOS-cursor](https://zhutix.com/ico/macos-cursor-21/)  
- Windows 鼠标指针：[Windows 指针主题](https://zhutix.com/ico/macos-cursor-21/)  

## 二、分平台配置方案

### 🔹 Windows

#### 1. 开机自启动
路径：  
```text
C:\ProgramData\Microsoft\Windows\Start Menu\Programs\StartUp
```
👉 建议将 **uTools**、**Snipaste** 等开机自启动的软件快捷方式放入其中。  

#### 2. PowerShell 脚本执行权限
以管理员身份运行 PowerShell：  
```powershell
set-executionpolicy remotesigned
```

#### 3. 安装 WSL
1. 参考安装教程：https://bbs.huaweicloud.com/blogs/350759  
2. 重启后，在 Microsoft Store 中安装：  
  [Windows Subsystem for Linux](https://apps.microsoft.com/store/detail/windows-subsystem-for-linux/9P9TQF7MRM4R)  

#### 4. PowerShell 美化（Oh My Posh）
1. 安装 [Oh My Posh](https://ohmyposh.dev/)  
2. 写入 `$PROFILE`：  
```powershell
oh-my-posh init pwsh --config $env:POSH_THEMES_PATH\M365princess.omp.json | Invoke-Expression
```
3. 字体推荐：**CaskaydiaCove NFM**  
4. 查看主题：  
```powershell
Get-PoshThemes
```

---

### 🔹 macOS

#### 1. Homebrew
安装 [Homebrew](https://brew.sh/)，便于软件包管理。  

#### 2. Zsh 美化
推荐使用 **[Starship](https://starship.rs/)**：  

```bash
brew install starship
```
zsh 配置：
```bash
# ~/.zshrc
# Starship 初始化
if command -v starship >/dev/null 2>&1; then
  eval "$(starship init zsh)"
fi
```
执行 `source ~/.zshrc` 使配置生效

##### 配置 Starship
创建 `~/.config/starship.toml` 文件并添加以下内容：
```toml
# ~/.config/starship.toml
format = """
$username\
$git_branch\
$nodejs\
$character"""
# 在提示符之间插入空行
add_newline = false

# 使用内置的username模块而不是env_var
[username]
style_user = "#17A8CD"
format = "[$user](#17A8CD):"
show_always = true

[git_branch]
# 显示当前的 Git 分支
format = "[$branch](#D670D6) "

[nodejs]
# 显示 Node.js 版本
format = "[$version](#23D184) "

[character]
# 确定提示符字符的显示，根据上次命令的成功或失败显示不同的颜色
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"
```

此配置会显示用户名、Git分支、Node.js版本，并根据命令执行结果显示不同颜色的提示符。

---

### 🔹 Linux (Ubuntu / Debian 示例)

#### 1. 更新系统
```bash
sudo apt update && sudo apt upgrade -y
```

#### 2. 安装常用工具
```bash
sudo apt install -y curl git vim build-essential
```

#### 3. 安装 Zsh + Starship
```bash
sudo apt install -y zsh
sh -c "$(curl -fsSL https://starship.rs/install.sh)"
echo 'eval "$(starship init zsh)"' >> ~/.zshrc
chsh -s $(which zsh)
```

#### 4. Node.js 环境配置
按照第一部分中的 NVM 安装指南配置 Node.js 环境。  

---

## 三、字体推荐
- [MiSans](https://web.vip.miui.com/page/info/mio/mio/detail?postId=33935854)  
- [Recursive](https://github.com/arrowtype/recursive)  
- CaskaydiaCove NFM （推荐给 PowerShell / VSCode 使用）

---

## ✅ 总结
本指南分为 **Windows / macOS / Linux 三套独立方案**：

- 🪟 Windows → PowerShell + WSL + Oh My Posh
- 🍎 macOS → Zsh + Starship + NVM Node 管理
- 🐧 Linux → Zsh + Starship + 系统工具

按步骤逐一执行，即可快速完成新机器的开发环境初始化。
